<h2 class="text-center text-primanry">Publisher List</h2>

<% @publishers.each_with_index do |publisher, index| %>
	<h3><i><%= index + 1 %></i><%= link_to publisher.name, publisher_path(publisher) %></h3>
	<p><%= publisher.address %></p>
<% end %>
